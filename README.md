# Financial Fraud Analytics Pipeline

## Project overview

This project demonstrates an end-to-end analytics pipeline for financial transaction fraud monitoring.

The planned workflow includes:

```text
KaggleHub → mock banking API → Python ETL → star schema (Kinball model)→ SQL KPI queries → automated report → Power BI dashboard